<!-- Meta Pixel Code -->
<script is:inline>
  /* eslint-disable @typescript-eslint/no-unused-expressions, prefer-spread, prefer-rest-params */
  !(function (f, b, e, v, n, t, s) {
    if (f.fbq) return;
    n = f.fbq = function () {
      n.callMethod ? n.callMethod.apply(n, arguments) : n.queue.push(arguments);
    };
    if (!f._fbq) f._fbq = n;
    n.push = n;
    n.loaded = !0;
    n.version = "2.0";
    n.queue = [];
    t = b.createElement(e);
    t.async = !0;
    t.src = v;
    s = b.getElementsByTagName(e)[0];
    s.parentNode.insertBefore(t, s);
  })(window, document, "script", "https://connect.facebook.net/en_US/fbevents.js");
  if (!window.flameTheFreezeMetaPixelInitialized) {
    fbq("init", "1774128133609281");
    window.flameTheFreezeMetaPixelInitialized = true;
  }
  if (!window.flameTheFreezeMetaPixelPageViewListener) {
    window.flameTheFreezeMetaPixelPageViewListener = () => fbq("track", "PageView");
    document.addEventListener("astro:page-load", window.flameTheFreezeMetaPixelPageViewListener);
  }
  if (!window.flameTheFreezeMetaPixelClickListener) {
    // Campaign conversion names: FlyerDownloadClick and ZoomRegistrationClick.
    const trackedMetaPixelEvents = new Set(["FlyerDownloadClick", "ZoomRegistrationClick"]);
    window.flameTheFreezeMetaPixelClickListener = (event) => {
      const trackedLink = event.target?.closest?.("[data-meta-pixel-event]");
      const eventName = trackedLink?.getAttribute?.("data-meta-pixel-event");
      if (eventName && trackedMetaPixelEvents.has(eventName)) fbq("trackCustom", eventName);
    };
    document.addEventListener("click", window.flameTheFreezeMetaPixelClickListener);
  }
  /* eslint-enable @typescript-eslint/no-unused-expressions, prefer-spread, prefer-rest-params */
</script>
<noscript
  ><img
    height="1"
    width="1"
    style="display:none"
    src="https://www.facebook.com/tr?id=1774128133609281&ev=PageView&noscript=1"
  /></noscript
>
<!-- End Meta Pixel Code -->
